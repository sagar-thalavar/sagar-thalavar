This is a future plans doc.

## Features to Build

- **[PRIORITY] CSV File Reader** — A polished web tool to upload, parse, search, filter, and visualize CSV data files. To be deployed under a custom subdomain (e.g., `csv.sagarthalavar.in`).

- **Video Showcase** — Showcase a custom video co-created with a friend on the main portfolio page.

- **Payment gateway** — Integrate a payment gateway (e.g., Stripe or Razorpay test sandbox) to experience payment processing flows.

- ~~**Visitor counter**~~ ✅ Done — Supabase RPC increments on each visit; Eye icon + count shown top-left on homepage, mobile responsive.

- ~~**Admin auto-approve**~~ ✅ Done — Admin entries are instantly approved on submission via role check in `queries.ts` + Supabase DB trigger sets role.

- **Colorful theme** — A third theme option alongside dark and light mode, with a colorful design built from scratch with a personal touch.

- **10k users project** — Build a project that reaches at least 10,000 live users and add it to the portfolio.
